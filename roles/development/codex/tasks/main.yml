---
- name: Install codex (linux/macos)
  become: true
  community.general.npm:
    name: "@openai/codex"
    unsafe_perm: true
    global: true
    state: latest
  environment:
    PATH: "/opt/homebrew/opt/node@{{ node.version.split('.')[0] }}/bin:{{ ansible_env.PATH }}"
